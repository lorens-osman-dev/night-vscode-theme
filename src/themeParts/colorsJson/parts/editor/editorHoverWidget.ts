import { alpha, colors } from "@/themeParts/colorsObject/colorsObject";

const editorHoverWidget = {
  "editorHoverWidget.foreground": colors.nightVali.foreground,
  "editorHoverWidget.background": alpha(colors.nightVali.BackgroundSecond, 0.827),
  "editorHoverWidget.border": colors.nightVali.background,
};

export default editorHoverWidget;
